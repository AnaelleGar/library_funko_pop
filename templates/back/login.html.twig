{% extends 'base.html.twig' %}
{% block body %}
    <h1>{{ 'login'|trans({}, 'Login') }}</h1>
    {{ form_start(loginForm) }}
        {{ form_rest(loginForm) }}
     <button type="submit">{{ 'validate'|trans({}, 'Login') }}</button>
    {{ form_end(loginForm) }}
    <a href="{{ path('back_sign_up') }}">S'inscrire</a>
{% endblock %}